schema_version: "1.0"
current_version: "1.0"
supported_versions:
  - "1.0"
allowed_transitions:
  - from: "1.0"
    to: "1.0"
    mode: noop
notes:
  en: Migration starts with planning mode only; apply transitions when version 1.1 is introduced.
  pt-BR: Migracao inicia apenas em modo de planejamento; aplicar transicoes quando a versao 1.1 for introduzida.
