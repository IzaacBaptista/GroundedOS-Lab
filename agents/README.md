# Agent Profiles

This folder defines agent role behavior for planning, implementation, and review.

Current profiles:

- `planner.yaml`
- `implementer.yaml`
- `reviewer.yaml`

These files are metadata for orchestration and prompting, not runtime agent code.
Runtime implementation remains under `packages/agents/`.
