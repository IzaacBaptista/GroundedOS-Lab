# Skills

This folder contains skill definitions and routing metadata for recurring task
intents.

Primary entrypoint:

- `registry.yaml`

Each skill should declare:

- intent patterns
- preferred agent role
- prompt template
- eval profile
